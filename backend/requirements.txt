# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20   # required for Form()/File() uploads

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0     # required by pydantic's EmailStr

# --- Auth / security ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
slowapi==0.1.9              # rate limiting

# --- Database ---
motor==3.7.1                # async MongoDB driver
pymongo>=4.15,<5.0          # explicit newer pin -- older pymongo (via older motor) has
                             # a known TLS handshake issue (TLSV1_ALERT_INTERNAL_ERROR)
                             # against MongoDB Atlas

# --- RAG / LLM ---
# Groq only for chat -- no Gemini/OpenAI. Embeddings run locally via
# sentence-transformers (no external embeddings API, no key, no billing --
# Groq itself has no embeddings endpoint at all).
chromadb==0.5.23
sentence-transformers==3.3.1
langchain-core>=0.3.33,<0.4.0
langchain-text-splitters>=0.3.5,<0.4.0
langchain-groq>=0.2.4,<0.3.0
pypdf==5.1.0