# Leave this blank for local development (npm run dev) -- vite.config.ts
# already proxies /api to http://localhost:8000 for you.
#
# Only set this when the frontend and backend are NOT served from the same
# origin, e.g. if you deploy them separately without the provided
# docker-compose/nginx setup.
VITE_API_BASE_URL=
